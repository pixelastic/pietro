You don't have `convert` available on your system.

`convert` is part of the ImageMagick suite and is used to convert pdf to images.

You can install it on Ubuntu using:
`sudo apt-get install imagemagick`
