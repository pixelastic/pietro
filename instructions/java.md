You don't have `java` available on your system.

`java` is needed to run Apache Tika, used to extract text from PDFs.

You should be able to install it on Ubuntu using:
`sudo apt install openjdk-11-jdk`
